# Active ticket pointer — each developer sets their own locally
.ai/tickets/active.md

# Per-developer tracker config — each developer sets their own locally
.claude/project.json

# Per-ticket run traces (JSONL) — local-only, regenerated by session hooks
.ai/runs/

# Eval runs (transcripts + run-id directories) — local-only
evals/runs/

# Per-ticket worktrees — created by /tdd, removed after PR merge
.ai/worktrees/

# Node
node_modules/

# OS
.DS_Store
workflows/
